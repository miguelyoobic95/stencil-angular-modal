{
  "name": "duplicate-bug",
  "private": false,
  "version": "0.0.0",
  "description": "Duplicate bug with stencil and angular",
  "files": ["dist/"],
  "scripts": {
    "start":
      "cd my-component  && npm run build && cd ../angular && ng serve --open",
    "install": "cd my-component && npm install && cd ../angular && npm install"
  }
}
